import styled from "styled-components";

export const StyleTitleModal = styled.input`
  font-weight: var(--font-weight-4);
  font-size: var(--font-size-4);
  color: var(--font-color-2);
  
  @media (orientation: portrait) {
  }
`;
